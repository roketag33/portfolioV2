name: Sync Button Showcase to Child Repo

on:
  push:
    paths:
      - 'src/app/[locale]/lab/buttons/**'
      - 'src/components/lab/buttons/**'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Filter and Move Files
        run: |
          mkdir -p temp_deploy
          
          mkdir -p temp_deploy/src
          
          # Copy App Page
          cp -r src/app/[locale]/lab/buttons temp_deploy/src/page
          
          # Copy Components
          cp -r src/components/lab/buttons temp_deploy/src/components

          # Create a README
          echo "# Button Showcase" > temp_deploy/README.md
          echo "Advanced UI interaction gallery built with React and Framer Motion." >> temp_deploy/README.md

      - name: Push to Child Repository
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.API_TOKEN_GITHUB }}
        with:
          source-directory: 'temp_deploy'
          destination-github-username: 'roketag33'
          destination-repository-name: 'lab-buttons'
          user-email: alexandre.sarrazin@gmail.com
          target-branch: main
          create-target-branch-if-needed: true
